<h1>View formulas by category:</h1>

<h2>Face</h2>
<% @face_categories.each do |category| %>
  <h4><%=link_to category.name, category_path(category)%></h4>
<% end %><br>

<h2>Hair</h2>
<% @hair_categories.each do |category| %>
  <h4><%=link_to category.name, category_path(category)%></h4>
<% end %><br>

<h2>Body</h2>
<% @body_categories.each do |category| %>
  <h4><%=link_to category.name, category_path(category)%></h4>
<% end %>